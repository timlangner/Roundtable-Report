## Summary

## Test plan

- [ ] `cmake --build build --config Release`
- [ ] `ctest --test-dir build -C Release --output-on-failure`
- [ ] No Discord webhook URLs in the diff
- [ ] No files under `docs/superpowers/`
